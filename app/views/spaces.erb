<!DOCTYPE html>
<html>
  <head>
    <title>Makersbnb</title>
    <script src = 'jquery-3.2.1.min.js'></script>
    <script src = 'interface.js'></script>

  </head>

  <body>
    <h1>Makersbnb<h1>
    <h2>Spaces<h2>

    <% if current_user %>
    <form action="/spaces/my-spaces/new">
      <input type="submit" value="Create a space">
    </form>
    <% end %>


    <ul id='spaces'>
      <% @spaces.each do |space| %>
        <li>
          <p>Name:
            <a href = '/spaces/<%= space.id %>'><%= space.name%></a></p>
          <p>Description:
            <%= space.description %></p>
          <p>Price: £<%= format('%.2f', space.price) %></p>
          <% if @mine %>
            <input type="button" value="View bookings">
            <div id="bookings">
              <% space.bookings.select{|booking| booking.confirmed == false}.each do |booking| %>
                <%= booking.start_date %> -- <%= booking.end_date %> booked by <%= booking.user.username %>
                <input type="button" value="Confirm">
              <% end %>
            </div>
          <% end %>
        </li>
      <% end %>
    </ul>

    <button id= 'refresh' >Refresh</button>

  </body>
</html>
